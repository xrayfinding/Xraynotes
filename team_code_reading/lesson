publish
balance controller manager
  调用硬件。
  sa.handle检测外部触发停止。

ethecat
  加载参数
  读取
  注册到hardware_interface;
  gethandle得到地址；
  hwiterface(regesiterhandle) -->  controler.(gethandle)

  init
  扫描配置从站
  canopen的运动控制协议，ethecat传输。
  ANYdrive不支持同时读写；
  read
    SDO--（配置）》PDO
  write
    选择模式

  controller：
    得到地址
    取电机状态、接触状态

  身体平衡控制器：
    初始化
    单腿控制；
    身体；

    通过handle读取hardwareinterface的值
    
